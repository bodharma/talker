services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: talker
      POSTGRES_PASSWORD: talker
      POSTGRES_DB: talker
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
